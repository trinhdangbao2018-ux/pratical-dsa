/*
 * Problem: <name>
 * Source:  <link>
 *
 * Approach:
 *   <one-paragraph description of the idea>
 *
 * Time:  O(?)
 * Space: O(?)
 *
 * Notes:
 *   <gotchas, edge cases, alternative approaches considered>
 */

#include <bits/stdc++.h>
using namespace std;

/* ---- solution ---- */

/* ---- example driver ---- */
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    /* sample input → expected output, asserted */
    return 0;
}
